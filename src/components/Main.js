import React from "react";
import Button from "./Button";

export default function Main() {
  return (
    <div>
      Main content
      <Button name="Submit" />
      <Button name="Cancel" />
      <Button name="Reset" />
    </div>
  );
}
